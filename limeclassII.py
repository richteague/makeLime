"""
Class to hold all the variables for running LIME models.
Part of the LIMEpy packge.
"""

# import os
# import headerclass as inputmodel
# import lamdaclass as rates
# import numpy as np


class definemodel:

    def __init__(self, **kwarsg):
        """Determine the parameters to make models."""

        # Split the inputs into three main sections: i) input model related
        # values, ii) radiative transfer properties, e.g. the par-> values, and
        # iii) all the imaging parameters, e.g. the img[i] values.

        return
